### Debug 测试函数

### Import & Usage

```javascript
import { VDebug } from '@bilibili/sakura'
VDebug({
  isDev: false, // 当设置为 true 的时候，下面的功能才会生效
  css: false, // 当设置为 true 的时候，页面上的 dom 都会加上一个边框
  js: false, // 暂时没什么用
  version: '', // 填写你当前的版本号，就会在 console 看到版本提示
})
```

